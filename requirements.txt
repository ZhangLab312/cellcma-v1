# CellCMA — frozen dependency list
# ---------------------------------------------------------------------------
# Captured from the development/test environment:
#   OS      : Linux x86-64 (Ubuntu-class)
#   Python  : 3.12.3
#   CUDA    : 12.8   (NVIDIA driver compatible with CUDA 12.8)
#   PyTorch : 2.7.0+cu128
#
# Install order:
#   1. python -m pip install -U pip setuptools wheel
#   2. python -m pip install torch==2.7.0+cu128 torchvision==0.22.0+cu128 \
#          --index-url https://download.pytorch.org/whl/cu128
#   3. python -m pip install -r requirements.txt
#
# (For a CPU-only build, replace step 2 with:
#    python -m pip install torch==2.7.0 torchvision==0.22.0 \
#        --index-url https://download.pytorch.org/whl/cpu)
# ---------------------------------------------------------------------------

# --- Framework core (directly imported by the CellCMA source) -------------
anndata==0.12.10
numpy==2.1.3
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.8.0
imbalanced-learn==0.14.1
aiohttp==3.13.3
requests==2.33.1
python-dotenv==1.2.2
biopython==1.86
pyyaml==6.0.2
psutil==6.1.0
matplotlib==3.9.2

# --- Single-cell analysis stack (used by LLM-generated analysis code) -----
# The agent generates and executes analysis code at runtime; these packages
# provide the methods (clustering, integration, GRN, perturbation) it may call.
scanpy==1.12
scvi-tools==0.20.3
scgpt==0.2.4
scib==1.1.7
mudata==0.3.3
leidenalg==0.11.0
igraph==1.0.0
networkx==3.4.2
umap-learn==0.5.12
rdkit==2026.3.1
statsmodels==0.14.6
seaborn==0.13.2
h5py==3.16.0
pyarrow==24.0.0
openpyxl==3.1.5

# --- Deep-learning backends ----------------------------------------------
# torch is installed separately (see header) for the matching CUDA build.
pytorch-lightning==1.9.5
lightning==2.6.1
torchmetrics==1.9.0
torchvision==0.22.0+cu128
jax==0.10.0
jaxlib==0.10.0
flax==0.12.7

# --- LLM / agent client stack --------------------------------------------
openai==2.29.0
anthropic==0.86.0
tiktoken==0.12.0
httpx==0.27.2

# --- Bio-data retrieval & utilities --------------------------------------
biothings-client==0.5.0
mygene==3.2.2
cell-gears==0.0.2
tqdm==4.67.3
joblib==1.5.3
